# 阶段 4.1 · 联调：核心闭环先行（商家端 mock → 真实 API）

> 归档日期：2026-08-25 ｜ 阶段：✅ 完成
> 续接依据：本归档 + `进度总表.md`；不靠对话记忆。
> 范围：按用户选定「核心闭环先行」——商家端核心视图（登录/桌台图/代客点单/订单/KDS/沽清/对账/日志/退款）对真实后端可用；管理配置类视图仍 mock 并标注「联调未覆盖」。

## 交付物

后端补齐商家读端点 + 前端数据层原位替换为真实 API + 核心视图写动作接线，双端编译 + 后端 43 项冒烟 + 代理链路 e2e 全过。

| 项 | 内容 |
|---|---|
| 后端读端点 | `GET /admin/tables`、`/admin/orders`、`/admin/dishes`、`/admin/kitchen/orders`、`/admin/refunds`（金额一律**分**，权限按 16.2 分流，越权 40003） |
| KDS 卡片流转 | `POST /admin/kds/:id/move {to:preparing\|served}`（后厨组 kitchen\|owner，7.4 §1982）：联调测试暴露设计缺口（2.4 未实现）后补齐；与收银台 `changeOrderStatus` 共用状态机，但**权限分离**——后厨只能走本端点，走 `/orders/:id/status` 越权 40003 |
| 代客收款 | `POST /admin/orders/:id/cashier-pay {channel:cash\|pos\|scan}`：先付单现金/POS/扫码即时收款，幂等（已付拒 20010/20013）；补 Payment + 订单置 paid |
| 登录补 id | `POST /auth/staff/login` 响应补 `id`（前端 Operator 需要）；`GET /admin/logs` join employees 补 `operator_name` |
| 前端数据层 | `src/api/mock.ts` **原位替换**（函数签名不变、视图数据逻辑不动）：7 个读函数走 `http`，`_cents→元` 在适配层完成；新增 8 个动作函数；保留 dashboard/manual/打印/员工/备份/补录 mock（联调未覆盖） |
| 前端认证 | `stores/auth.ts` mockLogin → `realLogin`（POST /auth/staff/login）；`Login.vue` 角色选择改「演示账号预填」，实际权限以后端返回为准 |
| 核心视图接线 | TableMap 结账/清台/核销 → `createBillPay`/`closeSession`/`writeOffSession`；Pos 下单 → `submitOfflineOrder` + 先付 `cashierPay`；Kds 制作/出餐 → **按角色分流**（`auth.role==='kitchen'` 走 `kdsMove`，否则 `changeOrderStatus`）；Soldout 沽清 → `setSoldOut`；OrderList/Recon/Log 仅靠适配层 |
| 审计留痕补齐 | 联调测试暴露 2.4 缺口：`session.close`/`session.write_off`/`order.refund`/`order.kds_move` 未落 `audit_logs`（违 16.2 敏感操作留痕）→ 各 handler 补 `middleware.RecordAudit`，Logs 视图可查 |

## 关键实现决策

- **读端点全部返回金额分、适配层折元**：后端与契约 7.4 一致用 int64 分；前端视图的 `¥{{ x.toFixed(2) }}` 不变，换算集中在 `mock.ts` 适配层，视图零改动。
- **代客收款补小端点而非复用会话级 bill/pay**：先付单是「单订单即时收款」（现金/POS/扫码），会话级 `bill/pay` 只服务后付统一结账——两态并存（3.7）。
- **后厨写状态走专用 KDS 端点而非收银台端点**：权限矩阵（16.2）后厨「接单/出餐」≠ 收银「改状态」；7.4 §1982 的 `kitchen.POST /kds/:id/move` 在 2.4 未实现，联调「后厨也走 `/orders/:id/status`」测试暴露此缺口。修复：新增 `/kds/:id/move`（kitchen|owner），后厨访问 `/orders/:id/status` 保持 40003。前端 Kds.vue 按 `auth.role` 分流。
- **敏感操作审计留痕补齐**：16.2 要求结账/退款/核销/清台/沽清/代客收款留痕 `audit_logs`；2.4 的 `session.close`/`write_off`/`order.refund` handler 漏调 `RecordAudit`（联调测试断言 `session.close` 落库失败暴露）。修复：三处 handler + 新 `KdsMove` 全部补 `RecordAudit`（动作 `session.close`/`session.write_off`/`order.refund`/`order.kds_move`）。
- **后付单 KDS 无后端状态**：状态机（7.2 B）`unsettled→paid→done` 无 `preparing/served`；KDS 对后付单的「开始制作/完成出餐」仅本地标记（不写后端），先付单才 `changeOrderStatus`。这是诚实取舍，不是缺陷。
- **Pos 外带（TID=0）后端不支持**：`createOrder` 需有效桌台（`db.First(&t, ...)`），Pos 外带场景弹提示「后端未支持」，联调覆盖桌台代客点单。
- **按人项由服务端按 pax 自动计入**：`/admin/dishes` 不返回 per_head 菜品；Pos 清单不再带 per_head 行，改由 `pax` 触发后端自动加餐位费等（3.8），Pos `pax` 取清单内 per_head 数量或默认 2。
- **对账六口径由 ReconSnapshot 转换**：`order_receivable+bill_receivable`=订单口径、`payment_by_channel` 分渠道=网关/渠道口径、现金/POS/扫码=收银口径、退款/净额=财务口径、挂账/核销单独列示、`diff`（应收-实收）做校验行——视图的 `diffCount/totalDiff` 语义自动对上。

## 验收证据

```
后端 43 项冒烟（/tmp/smoke_41.sh，prepay A02 + postpay B01）：
  staff login 1001/123456 → token+id=1+role=owner；1003 kitchen 越权 /admin/tables → 40003
  B01(postpay) 顾客下单挂账 → tables session.unsettled_cents=12800 / orders 含该单（source=online, items price_cents）
  A02(prepay) 代客点单 → cashier-pay cash → paid + amount 一致；重复收款拒 20010/20013
  dishes 18 道（分类/分/沽清/unit 默认份）；kitchen 含 unsettled+paid、无 per_head 行
  退款 → refunds 含该单（requested_by=张店长、order_no 非空、amount 一致）
  logs join operator_name + order.cashier_pay 动作；bill/pay cash → settled → wait_clean=true

前端编译（merchant-web，vue-tsc + vite）：npm run build → ✓ built（零类型错误）
视图动作链 e2e（/tmp/e2e_41_view.sh，vite :5173 → :8080，等价浏览器请求）25/25 PASS：
  0  登录：owner 1001 + kitchen 1003
  1  TableMap 读 + Pos 代客下单 A03(prepay) → cashierPay pos → paid、金额含按人项一致
  2  KDS：owner 收银台改状态 preparing ✓；后厨走 /orders/:id/status → 越权 40003 ✓；
     后厨走 /kds/:id/move → served ✓（权限分离成立）
  3  Soldout 沽清/恢复 + dishes 校验
  4  Refund 单步退款 success → refunds 列表含该单
  5  TableMap 结账 B02(postpay) bill/pay cash → 清台 → 桌台 empty
  6  Logs 留痕：order.status_change / order.kds_move / order.refund / dish.soldout /
     session.close / order.cashier_pay / bill.pay 全部可查（16.2 敏感操作留痕）
代理链路 e2e（vite :5173 → :8080）：GET / → 200；login token；tables 3 区；dishes 18；logs operator_name
服务端 go vet ./... + go build ./... 通过（阶段 4.1 补丁后重启，health db/redis ok）
```

**未验证项（留待宿主人工）**：浏览器实际交互走查（桌台图结账/清台弹窗、代客点单收款、KDS 制作按钮、沽清点击、登录跳转）。Windows 宿主浏览器为人工检查（与 3.4/3.5 同口径）；WSL 内以「双端编译 + 代理链路数据正确 + 后端契约冒烟全过」为可验证证据。

## 已知取舍 / 联调未覆盖清单

- **并桌 merged**：后端无并桌表/字段 → `merged:false`，桌台图「⊕」不出现。
- **档口 station / 单位 unit / 按人项 item_type**：`Dish` 表无这些字段 → 适配层默认 `station:''`/`unit:'份'`/`item_type:'dish'`；KDS 档口筛选退化为「全部」。待后端菜品管理 API。
- **订单 remark / KDS area / Order.sessionId**：后端订单/厨房端点未返回 → 适配层给空/0，视图未消费（诚实，不造假）。
- **外带 TID=0**：后端 `createOrder` 需有效桌台 → Pos 外带提示「后端未支持」。
- **退款审批流**：后端为**单步退款**（请求即处理，success→'done'），Refund 视图的「待审批/批准/驳回」UI 在后端语义下休眠（无 pending 行可操作），前端未删除（待后端补审批流再激活）。
- **管理配置视图**（Dashboard/Manual/PrintTasks/Staff/Backups/Offline/ServiceCalls + Menu/Tbmgr/Qrcode/Perhead 的写动作）：仍 mock，标注「联调未覆盖，待后端管理 API」；共享的 `fetchDishes`/`fetchTableAreas` 已随核心视图变真实。
- **KDS 落单计时**：`placedAt` 仍由前端按列表序模拟（`base - i*75s`），服务端无落单时间戳返回。
- **真实支付 SDK / 打印 / 语音蜂鸣**：阶段 4 后续（部署「202」拓扑见设计 15.8）。

## 下一步

- 阶段 4.2 候选：管理配置类视图补后端 API（菜品/桌台/员工/对账导出）或直接进入部署「202」+ 备份容灾演练（设计 15.8 / 第 12 章）。
