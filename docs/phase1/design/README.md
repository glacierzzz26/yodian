# 阶段 1 · 商家端 Web 设计定稿索引

> 状态：定稿（以 `docs/商家端原型.html` 为视觉契约，`docs/扫码点餐系统设计方案.md` v1.8 为业务契约）

## 定稿依据

| 契约 | 文件 | 覆盖 |
|---|---|---|
| 视觉/交互 | `docs/商家端原型.html`（v1.0） | 全部 22 页 + KDS 大屏的布局、组件、配色 |
| 业务规则 | `设计方案.md` 第 10/11 章（商家端/后台设计）、3.7（三态结账）、5（降级对账）、16.2（权限矩阵） | 页面行为与数据口径 |
| 接口契约 | `设计方案.md` 第 7.4 / 16.1 | 响应信封 `{code,msg,data}`、错误码、字段形状 |

## 技术栈（设计文档 1/14.2 定稿）

- Vue 3（`<script setup>`）+ Vite + TypeScript + Pinia + vue-router
- UI：Ant Design Vue 4（商家端亮色 / KDS 暗色 `darkAlgorithm`）
- 请求：axios，统一响应包络解包；mock 层先占位，契约不变

## 页面清单（22 + KDS）

| 分组 | 页面 | 路由 |
|---|---|---|
| 登录 | 登录（工号/密码/三角色） | `/login` |
| 收银工作台 | 营业概览 / 桌台图 / 订单管理 / 代客点单 / 补录人工单 / 需人工处理 | `/dashboard` `/tables` `/orders` `/pos` `/offline` `/manual` |
| 后厨 | KDS 大屏 / 打印任务 | `/kds` `/print` |
| 菜单与桌台 | 分类与菜品 / 按人收费项 / 沽清管理 / 批量导入 / 桌台管理 / 二维码管理 | `/menu` `/perhead` `/soldout` `/import` `/tbmgr` `/qrcode` |
| 报表与对账 | 营业报表 / 多来源对账 / 退款与核销 | `/report` `/recon` `/refund` |
| 系统 | 员工与权限 / 操作日志 / 系统设置 / 备份与恢复 | `/staff` `/log` `/setting` `/backup` |

## 角色权限（16.2 权限矩阵 → 路由守卫 + 菜单可见性）

| 角色 | 可见范围 |
|---|---|
| 收银员 cashier | 收银工作台（除退款/核销/降级/对账）+ 报表查看 |
| 后厨 kitchen | 仅 KDS 大屏 + 打印任务 |
| 店长 owner | 全部 |

## 本阶段执行状态

- [x] 阶段 0 地基（仓库/文档/本表）
- [x] 1.1 脚手架 + 登录 + 外壳 + 角色路由
- [x] 1.2 收银工作台 + 后厨核心页
- [x] 1.3 管理后台页

阶段小节归档见 `docs/phase1/stages/`。
